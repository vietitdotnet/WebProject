@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Xác minh mã khôi phục";
}

<div class="container">
    <div class="form-login">
        <h1 class="title-manager"><i class="bi bi-shield-lock"></i> @ViewData["Title"]</h1>
        <hr />
        <p class="alert alert-warning">
   
        Bạn đã yêu cầu đăng nhập bằng khôi phục mã hóa. Thông tin đăng nhập này sẽ không được ghi nhớ khi bạn cung cấp xác thực ứng dụng mã hóa khi đăng nhập hoặc tắt 2FA và đăng nhập lại.
        </p>
        <div class="row">
            <div class="col-md-4">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="RecoveryCode" />
                        <label asp-for="Input.RecoveryCode" class="form-label">Nhập mã</label>
                        <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                    </div>
                    <button type="submit" class="w-100 btn btn-primary">Đăng nhập</button>
                </form>
        </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
    </div>
</div>
